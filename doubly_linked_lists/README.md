 a new readme for doubly linked list 
in data structre the doubly linked contain three fields 
1 - data 
2 - a pointer to the next nod (next)
3 - a pointer to the previous nod (prev)

/* Each node in a Doubly Linked List contains the data it holds, a pointer to the next node in the list, and a pointer to the previous node in the list.*/
